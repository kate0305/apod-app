import classes from './main.module.css';

export const MainPage = () => (
  <main className={classes.wrapper}>
    <h1 className={classes.title}>Astronomy Picture of the Day</h1>
    <h2 className={classes.subtitle}>Discovery the cosmos!</h2>
  </main>
);
